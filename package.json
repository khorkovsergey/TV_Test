{
  "name": "expert-marketplace",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Concierge-MVP биржи проверенных консультантов: заявка → AI-бриф → подбор → бронирование → стандартизированный итог",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "check": "node --check src/server.js && node --check src/db.js && node --check src/claude.js && node --check src/prompts.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.70.0",
    "express": "^4.21.2",
    "pg": "^8.13.1"
  }
}
